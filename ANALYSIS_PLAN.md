# 의대 증원 여론 분석 프로젝트 - 분석 계획서

> 크롤링 키워드: `의대 증원` | 플랫폼: 네이버 블로그 + 카페 | 총 문서 수: 8,972건

---

## 1. 프로젝트 개요

### 1.1 분석 목적

2024년 2월 의대 2,000명 증원 공식 발표 이후 형성된 온라인 여론의 **시계열 변화**를 추적한다.
단순 찬반 집계가 아닌, 토픽·군집·감성 분석을 결합하여 **담론 구조의 진화**를 규명한다.

### 1.2 분석 구간

| 구간 | 기간 | 주요 사건 |
|------|------|-----------|
| 1구간 | 2024.01 ~ 03 | 의대 2,000명 증원 공식 발표 + 초기 충격 반응 |
| 2구간 | 2024.04 ~ 06 | 전공의 집단 사직 본격화 + 의료 공백 체감 확산 |
| 3구간 | 2024.07 ~ 12 | 사태 장기화 + 법적 공방 + 사회적 피로감 축적 |
| 4구간 | 2025.01 ~ 06 | 증원 입학생 수업 시작 + 전공의 복귀 여부 + 여론 재점화 |

### 1.3 데이터 현황

| 항목 | 값 |
|------|-----|
| 분석 문서 수 | 8,972건 |
| 원본 파일 | `combined_section_sorted_flat_comments.pkl` |
| 형태소 분석기 | kiwipiepy (명사 추출) |
| 불용어 수 | 221개 (`analysis_stopwords_excluded.txt`) |
| 불용어 제거 후 상위어 | 치료·미래·투자·주장·발생·간호사·시스템·아이·현장·최고 |

---

## 2. 전처리 완료 현황

> **현재 단계: 불용어 처리까지 완료**

```
[완료] 크롤링          →  naver_crawler.py
[완료] 컬럼 정규화     →  culumn_name_same.py
[완료] 텍스트 정제     →  preprocess_for_jupyter.py
                          · HTML/URL/이모지 제거
                          · ㅋㅋ/ㅎㅎ 처리
                          · 중복 게시글 제거 (80% 유사도 기준)
[완료] 형태소 분석     →  blog_cafe.ipynb
                          · kiwipiepy 명사 추출
                          · 댓글 병합·평탄화
[완료] 워드클라우드    →  구간별 Raw / 불용어 제거 후 시각화
[완료] TF-IDF 분석    →  구간별 핵심 키워드 30개 추출
[완료] 불용어 처리     →  221개 불용어 최종 적용
[진행중] K-Means      →  k=3 확정, 전체 학습 → 구간별 분포 집계
[진행중] LDA          →  전체 학습 5개 토픽 → 구간별 분포 집계
[ 예정 ] 감성 분석     →  감성 분류 + 시계열 변화 해석
```

---

## 3. 분석 단위 결정: 전체 학습 후 구간별 집계

### 3.1 핵심 질문

> K-Means와 LDA를 **4개 구간으로 나눠서** 각각 돌릴 것인가, 아니면 **전체 8,972건을 합쳐서** 한 번 돌릴 것인가?

### 3.2 구간별 분리 학습의 문제점

구간별로 나눠서 모델을 따로 학습하면 **각 구간이 독립된 토픽/군집 공간**을 가지게 된다.

예를 들어 K-Means(k=3)를 구간마다 따로 돌리면:

```
구간 1의 Cluster 0 = "소아과 붕괴 우려" 담론
구간 2의 Cluster 0 = "전공의 집단 사직" 담론   ← 완전히 다른 주제
```

두 Cluster 0은 같은 ID를 공유하지만 전혀 다른 의미를 가진다.
즉, **"구간 1의 Cluster 0이 구간 2에서 줄었다"는 비교 자체가 성립하지 않는다.**
시계열 변화를 보려고 구간을 나눴는데, 정작 구간 간 비교가 불가능해지는 모순이 생긴다.

LDA도 동일하다. 구간마다 따로 학습하면 "구간 1의 Topic 2"와 "구간 2의 Topic 2"가 서로 다른 주제를 가리키므로, 토픽이 시간에 따라 부상하거나 소멸했는지 추적할 수 없다.

### 3.3 전체 합산 후 구간별 집계 방식 (채택)

**전체 8,972건을 하나의 말뭉치로 학습**하면 모든 문서가 동일한 토픽/군집 공간을 공유한다.

```
[학습] 전체 8,972건 → K-Means / LDA 단일 모델 학습
         ↓
[할당] 각 문서에 Cluster ID / dominant Topic ID 부여
         ↓
[집계] section 컬럼으로 그룹핑
         ↓
[비교] 구간별 각 군집·토픽의 문서 비율 변화 시각화
```

이렇게 하면 "Cluster 0(의료 현장 우려)"이 구간 1에서는 40%, 구간 4에서는 15%처럼 **같은 레이블로 시계열 변화를 직접 추적**할 수 있다.

### 3.4 보완책: 구간별 미묘한 담론은 TF-IDF로 커버

전체 합산 시 특정 구간에서만 나타난 미묘한 담론이 묻힐 수 있다는 한계가 있다.
이는 이미 완료한 **구간별 TF-IDF 상위 키워드** 분석으로 보완한다.

| 분석 | 역할 |
|------|------|
| K-Means / LDA (전체 학습) | 전체 담론 구조 파악 + 구간별 비율 변화 추적 |
| TF-IDF (구간별) | 구간 고유의 키워드 변화 포착 |

---

## 4. K-Means 군집 분석

### 4.1 방법론

| 항목 | 설정 |
|------|------|
| 알고리즘 | KMeans |
| 군집 수(k) | **3** |
| 벡터화 | `TfidfVectorizer(min_df=5, max_df=0.85)` |
| 초기화 | `n_init=20, random_state=42` |
| **분석 단위** | **전체 8,972건 단일 학습** → 구간별 분포 집계 |
| 특징 수 | 전체 어휘 기준 (64,815개) |

**k=3 선택 근거**: 엘보우 분석(SSE vs K) 결과, k=3에서 SSE 감소율이 뚜렷이 꺾이는 지점이 확인됨.

### 4.2 분석 절차

```
1. 전체 doc_text_filtered (8,972건) 로딩
2. TF-IDF 행렬 생성 (전체 말뭉치 기준 희귀어·과다등장어 제거)
3. KMeans(k=3) 단일 학습 → 각 문서에 Cluster ID 할당
4. 군집 중심(centroid) 기준 상위 15개 키워드 추출 → 담론 유형 레이블링
5. section 컬럼으로 그룹핑 → 구간별 군집 비율 계산
6. 구간별 군집 비율 변화 시각화 (스택 막대 / 선 그래프)
```

### 4.3 출력 파일

| 파일 | 내용 |
|------|------|
| `outputs/pipeline/kmeans/kmeans_cluster_top_terms.csv` | 군집별 상위 키워드 + 가중치 |
| `outputs/pipeline/kmeans/kmeans_doc_assignments.csv` | 문서별 Cluster ID + section |
| `outputs/pipeline/kmeans/kmeans_section_distribution.csv` | 구간별 군집 비율 (시계열 추적용) |
| `outputs/pipeline/kmeans/kmeans_elbow.csv` | 엘보우 분석 SSE 수치 |

### 4.4 담론 유형 레이블링 계획

전체 학습 후 3개 군집의 centroid 키워드를 보고 **담론 유형** 레이블을 부여한다.

| 레이블 후보 | 대표 키워드 경향 |
|------------|----------------|
| 의료 현장 우려형 | 붕괴, 현장, 간호사, 근무, 환자, 소아청소년과 |
| 정책·교육 논쟁형 | 정원, 교육, 입학, 정부, 개혁, 법원 |
| 사회·경제 파급형 | 투자, 미래, 경쟁, 시스템, 비용, 반도체 |

레이블링 후 구간별 비율 변화를 통해 아래를 확인한다:
- 초기(구간 1)에는 어떤 담론이 지배적이었는가
- 사태 장기화(구간 3)에서 담론 분포가 어떻게 이동했는가
- 구간 4(복귀 국면)에서 새로운 담론이 부상했는가

---

## 5. LDA 토픽 모델링

### 5.1 방법론

| 항목 | 설정 |
|------|------|
| 알고리즘 | `LatentDirichletAllocation` (sklearn) |
| 토픽 수 | **5개** |
| 벡터화 | `CountVectorizer(min_df=5, max_df=0.85)` |
| 학습 방식 | `learning_method='batch', random_state=42` |
| **분석 단위** | **전체 8,972건 단일 학습** → 구간별 분포 집계 |
| 키워드 수 | 토픽당 상위 15개 |

### 5.2 LDA 수행 과정

```
1. 문서단어행렬(DTM) 생성
   - CountVectorizer로 전체 8,972건 벡터화
   - min_df=5: 5건 미만 등장 어휘 제외
   - max_df=0.85: 85% 이상 문서 등장 어휘 제외 (너무 흔한 단어)

2. 토픽 수 5 사전 설정

3. 모든 단어 → 5개 토픽 중 하나 무작위 초기 할당

4. 반복 최적화 (Batch EM)
   - 각 단어 제외 후 재할당:
     - 각 문서 내 토픽 분포 (θ) 계산
     - 각 토픽 내 단어 분포 (φ) 계산
   - 수렴까지 반복

5. 최종 출력
   - 각 문서의 dominant topic ID
   - 각 토픽의 상위 15개 키워드 + 가중치
   - section 컬럼으로 그룹핑 → 구간별 토픽 비율 집계
```

### 5.3 출력 파일

| 파일 | 내용 |
|------|------|
| `outputs/pipeline/lda/lda_topic_keywords.csv` | 토픽 ID·순위·키워드·가중치 |
| `outputs/pipeline/lda/lda_doc_topic_distribution.csv` | 문서별 dominant topic ID + section |
| `outputs/pipeline/lda/lda_section_distribution.csv` | 구간별 토픽 비율 (시계열 추적용) |

### 5.4 토픽 해석 계획

전체 학습 후 5개 토픽의 키워드를 보고 **토픽 레이블**을 부여한다.
같은 레이블 공간에서 구간별 비율 변화를 추적하여 담론 흐름을 파악한다.

| 분석 관점 | 질문 |
|----------|------|
| 구간별 지배 토픽 | 어떤 구간에서 어떤 토픽의 비율이 가장 높았는가? |
| 토픽 부상·소멸 | 구간 1→4로 가면서 특정 토픽이 증가하거나 사라졌는가? |
| LDA의 혼합 토픽 활용 | 단일 문서가 여러 토픽에 걸쳐 있을 때, 어떤 주제가 혼재하는가? |

---

## 6. LDA vs K-Means 비교 관점

두 분석은 **상호 보완적**으로 해석한다.

| 구분 | K-Means | LDA |
|------|---------|-----|
| 접근 방식 | Hard Clustering (1문서 = 1군집) | Soft Clustering (1문서 = 복수 토픽 확률) |
| 결과 형태 | 군집 중심 키워드 | 토픽별 단어 분포 |
| 강점 | 군집 크기·분리도 명확 | 혼합 토픽 문서 처리 유연 |
| 약점 | 혼합 주제 문서 오분류 가능 | 토픽 수 사전 설정 필요 |
| 활용 | 구간별 여론 클러스터 유형화 | 잠재 주제 발굴 + 시계열 변화 |

**해석 전략**: K-Means 군집 레이블과 LDA 토픽 레이블이 일치하면 해당 담론이 구조적으로 안정적임을 의미. 불일치 시 LDA의 혼합 토픽 특성을 활용하여 세부 분해.

---

## 7. 분석 노트북 실행 순서

```
notebooks/
└── preprocess_after_project.ipynb  ← 메인 분석 노트북
    │
    ├── [완료] Cell 7-8   : 데이터 로딩 (pkl)
    ├── [완료] Cell 9-10  : 명사 준비 (doc + comment 합산)
    ├── [완료] Cell 11-14 : Raw 워드클라우드 + 빈도 분석
    ├── [완료] Cell 15    : 불용어 221개 로딩
    ├── [완료] Cell 16-17 : 불용어 적용 + 필터링
    ├── [완료] Cell 18-19 : 필터링 후 워드클라우드
    ├── [완료] Cell 20-22 : TF-IDF 구간별 분석
    ├── [진행중] Cell 23-27 : K-Means (k=3, 전체 학습 → 구간별 집계)
    ├── [진행중] Cell 28-30 : LDA (5 topics, 전체 학습 → 구간별 집계)
    └── [예정]  Cell 31+  : 구간별 비율 변화 시각화 (스택 막대 / 선 그래프)
```

---

## 8. 설정값 요약 (`preprocess_after_project.ipynb`)

```python
# 현재 적용 중인 분석 설정
INCLUDE_TITLE_NOUNS = False       # 제목 명사 제외 (본문+댓글만)
TFIDF_TOP_N = 30                  # TF-IDF 상위 30개 키워드
KMEANS_N_CLUSTERS = 3             # 전체 학습 기준 k=3 (엘보우 분석 결과)
LDA_N_TOPICS = 5                  # 전체 학습 기준 토픽 수
LDA_TOP_WORDS = 15                # 토픽당 키워드 수

# 학습 후 구간별 분포 집계
# → 각 문서의 cluster_id / dominant_topic + section 컬럼으로 groupby
# → 구간별 비율 계산 및 시계열 시각화
```

---

## 9. 예상 최종 결과물

| 분석 | 출력물 | 해석 포인트 |
|------|--------|-------------|
| TF-IDF | `tfidf_top_terms_by_section.csv` | 구간별 핵심 키워드 변화 |
| K-Means | 군집 키워드 CSV + 구간별 비율 변화 그래프 | 3가지 여론 유형의 시계열 소장 추적 |
| LDA | 토픽 키워드 CSV + 구간별 비율 변화 그래프 | 5가지 잠재 담론의 부상·소멸 추적 |
| 워드클라우드 | 8장 PNG (구간×필터 조합) | 시각적 키워드 트렌드 |
| (예정) 감성분석 | 감성 레이블 + 시계열 그래프 | 긍정/부정 여론 추이 |

---

## 10. 다음 단계 (감성 분석 예정)

K-Means + LDA 완료 후 아래 순서로 감성 분석을 진행한다.

1. 감성 사전 기반 레이블링 (KNU 감성 사전 등)
2. 구간별 긍정/부정/중립 비율 계산
3. 군집·토픽별 감성 분포 교차 분석
4. 4개 구간 감성 트렌드 시계열 시각화
